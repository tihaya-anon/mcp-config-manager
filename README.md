# MCP Controller Extension

VS Code extension scaffold generated in current directory.

## Quick start

1. Install dependencies:
   npm install
2. Compile:
   npm run compile
3. Press `F5` in VS Code to launch Extension Development Host.

## Command

- `MCP Controller: Hello World` (`mcpController.helloWorld`)
